name: Tests CI

on:
  workflow_dispatch:
  push:

jobs:
  tests:

    runs-on: ubuntu-latest

    steps:
      - name: Check Code
        uses: action/checkout@v2
        with:
          java-version: '16'
          distribution: 'adopt'
      - name: build Jar
        run: mvn test